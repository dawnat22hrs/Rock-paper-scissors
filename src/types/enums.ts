export enum FINGERING_ITEM_SIZE {
    SMALL = 'small',
    LARGE = 'large',
}

export enum RESULT {
     WINNINGS = 'ВЫ ВЫИГРАЛИ',
    LOSS = 'ВЫ ПРОИГРАЛИ',
    DRAW = 'НИЧЬЯ'
}

export enum BUTTON_VARIANT {
    PRIMARY,
    GHOST,
}